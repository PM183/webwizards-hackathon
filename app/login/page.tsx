import { LoginForm } from '../../components/auth/LoginForm'

export const metadata = {
  title: 'Sign In - Polling System',
  description: 'Sign in to access the polling system',
}

export default function LoginPage() {
  return <LoginForm />
}